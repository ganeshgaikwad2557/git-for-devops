Hello kindly find below command 
Here's the list without the numbers:

```
ls
mkdir git-for-devops
cd git-for-devops/
pwd
git init
ls -a
git status
vim hello-dosto.txt
git status
touch nibbi.txt nibba.txt
ls
git add nibbi.txt
git status
git add nibba.txt
git status
git rm --cached nibba.txt
git status
\git add nibba.txt
git status
git commit -m "adding nibba nibbi" nibbi.txt nibba.txt
git config --global user.name "ganeshkgaikwad3"
git config --global user.email "ganeshkgaikwad3@gmail.com"
vi nibbi.txt
git sta
git status
git add nibbi.txt
git status
git commit -m "edite nibbi" nibbi.txt
r hello-dosto.txt
git status
rm hello-dosto.txt
git status
ls
git log
git checkout -b dev
vim nibbu.txt
git status
git add nibbu.txt
git commit -m "nibbu.txt" "add nibbu " nibbu.txt
git commit -m "add nibbu " nibbu.txt
git checkout master
git log
ls
git checkout dev
ls
git log
git checkout master
git log
git branch
git log --online
git log --oneline
git checkout dev
git log --oneline
history
```
